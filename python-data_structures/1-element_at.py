#!/usr/bin/python3
# retrieving an element from a list
def element_at(my_list, idx):
    if idx < 0:
         return 
    elif idx >= range(len(my_list)):
         return 
    else:
         return my_list[idx]
